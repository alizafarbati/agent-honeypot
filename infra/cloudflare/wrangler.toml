name = "agent-honeypot-edge"
main = "worker.mjs"
compatibility_date = "2024-11-01"

[[r2_buckets]]
binding = "HONEYPOT_R2"
bucket_name = "agent-honeypot-events"

[vars]
HONEYPOT_EDGE_NOTE = "synthetic-only, digest-only at edge"
